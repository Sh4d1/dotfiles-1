" config for when editing gopass/pass temporary files

" don't want to leak passwords over the system or have anything stored in
" history
set noundofile
set nobackup
set nowritebackup
set noswapfile
set viminfo=""
