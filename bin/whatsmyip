#!/usr/bin/env python
# -*- coding: utf-8 -*-
# vim: ft=python

import requests

try:
    res = requests.get('https://httpbin.org/ip', timeout=5)

    if res.status_code == 200:
        myip = res.json()['origin']
        print(myip)
except:
    print('error')
