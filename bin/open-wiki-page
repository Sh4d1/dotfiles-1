#!/usr/bin/env bash

# swalladge 2019
#
# Fuzzy find and open a private wiki page.

shopt -s nullglob globstar

cd "$HOME/wiki"
files=( **/* )

file=$(printf '%s\n' "${files[@]}" | rofi -dmenu -no-custom  -i -p "wiki ($MODE)" "$@")

[[ -n "$file" ]] || exit

exec urxvt -e nvim "$file"
